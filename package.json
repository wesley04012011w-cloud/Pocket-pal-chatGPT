{
  "name": "pocketpal-local",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "esbuild web/app.js --bundle --outfile=web/app.bundle.js",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4",
    "esbuild": "^0.25.0"
  }
}
