{
  "name": "pocketpal-local",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "esbuild web/app.jsx --bundle --outfile=web/app.bundle.js --loader:.jsx=jsx",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4",
    "esbuild": "^0.25.0"
  }
}
